to do...

Containers (Docker)

Il "Modern" nel nome della tua cartella si riferisce a questo.

Tools: Trivy (per scansionare le immagini Docker alla ricerca di CVE), Dive (per analizzare i layer delle immagini).

Lab: Esempi di Docker Socket mounting (una vulnerabilità classica che permette di diventare root sull'host ospitante).